version: '3'
   
services:
    # nginx
    nginx:
        image: nginx:1.10
        restart: always
        volumes:    
            - /docker/machine/volumes/nginx/conf.d/:/etc/nginx/conf.d:ro
            - /docker/machine/volumes/nginx/ssl/:/etc/nginx/ssl
            - /docker/projects:/applications
        ports: 
            - "80:80"
            - "443:443"

    # php 7.2
    php: 
        image: php:7.2-fpm
        restart: always
        volumes:
            - /docker/projects:/applications           

    # mysql
    mysql:
        image: mysql:5.7
        restart: always
        volumes:
          - /docker/machine/volumes/mysql:/var/lib/mysql
        ports: 
            - "3306:3306"
        environment:
            MYSQL_DATABASE: vdocker
            MYSQL_ROOT_PASSWORD: secret
        command: --innodb_use_native_aio=0
    
    #redis
    redis:
        image: redis:3.2
        restart: always
        ports:
            - "6379:6379"
            